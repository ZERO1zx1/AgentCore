# Example backend (`examples/backend/`)

Reference credit-accounting proxies for integrating AgentCore-style usage into an app.

- `fastapi/` — FastAPI proxy: `POST /api/agent/request`.
- `express/` — Express proxy: `POST /api/agent/request`.

These are minimal demos, not a production billing system.
