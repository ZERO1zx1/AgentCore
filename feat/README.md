# Feature experiments (`feat/`)

Proof-of-concept and experimental code, kept separate from the engine.

- `low_cost_skill/` — a low-cost skill POC (see its README). Not wired into `AgentCoreEngine`.
