# MCP (`src/mcp/`)

Model Context Protocol (MCP) stdio server exposing AgentCore over MCP.

- `server.py` — `run_stdio_server`.
- `__main__.py` — `python -m src.mcp` entry point.
- `__init__.py` — package exports.
