# Observability (`src/observability/`)

Shared read models used by the CLI, MCP, and other consumers so they agree on
manifest field names.

- `manifest_view.py` — projections such as `budget_view`, `manifest_prompt`, `task_summary`, `manifest_timestamp`.
- `__init__.py` — package exports.
