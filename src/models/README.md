# Models (`src/models/`)

Model registry and routing.

- `registry.py` — `ModelRegistry` with model specifications.
- `router.py` — capability- and budget-aware model routing.

The bundled registry is a demo; register production `ModelSpec` values in an
injected registry.
